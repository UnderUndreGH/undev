Execute the following implementation tasks for the spec "017-vpn-tab-deserialize-fix".

TASK 1 - T001 & T002: Fix response shape
- File: devops-app/server/routes/servers-vpn.ts
- What to do: Find the route for GET /api/servers/vpn and change `res.json({ servers: rows })` to `res.json(rows ?? [])`. Never pass potentially null/undefined rows directly.

TASK 2 - T002b: Check consumers
- What to do: Find consumers of `/api/servers/vpn` in the codebase (e.g., devops-app/client/lib/vpn-api.ts). Verify they expect an array and update them if they don't.

TASK 3: Update Tasks Status
- File: specs/017-vpn-tab-deserialize-fix/tasks.md
- What to do: Mark tasks T001, T002, T002b, and T003 as complete by changing `[ ]` to `[X]` in the markdown file.

Do not commit. When done, print a summary of the files modified.
